<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AlphaMobile.Views.RestaurantDetailsView">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label x:Name="RestoName" Text="" />
        <Label x:Name="MenuName" Text=""/>
        <ListView x:Name="listView"
                  IsGroupingEnabled="True"
                  GroupDisplayBinding="{Binding Title}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding Name}" Detail="{Binding Description}" ImageSource="{Binding ImageSource}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Refresh" Clicked="Button_Clicked"/>        
    </StackLayout>    
</ContentPage>